import React from "react"

function Header(props) {
    return (
        <header className="header">
            <h1 className="center-text header__heading">Brewery App</h1>
            <p className="center-text">Busca la cerveceria que necesites</p>
        </header>
    )
}

export default Header